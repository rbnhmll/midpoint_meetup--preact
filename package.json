{
  "private": true,
  "name": "midpoint_meetup--preact",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "start": "if-env NODE_ENV=production && npm run -s serve || npm run -s dev",
    "build": "preact build",
    "serve": "preact build && preact serve",
    "dev": "preact watch",
    "lint": "eslint src"
  },
  "eslintConfig": {
    "extends": "eslint-config-synacor"
  },
  "eslintIgnore": [
    "build/*"
  ],
  "devDependencies": {
    "eslint": "^4.5.0",
    "eslint-config-synacor": "^1.1.0",
    "if-env": "^1.0.0",
    "node-sass": "^4.6.0",
    "preact-cli": "^2.0.0",
    "preact-cli-plugin-async": "^1.0.0",
    "sass-loader": "^6.0.6"
  },
  "dependencies": {
    "axios": "^0.17.0",
    "font-awesome": "^4.7.0",
    "mapbox-gl": "^0.42.2",
    "preact": "^8.2.1",
    "preact-compat": "^3.17.0",
    "preact-router": "^2.5.5",
    "smoothscroll": "^0.4.0",
    "turf": "^3.0.14",
    "twitter-widgets": "^1.0.0"
  }
}
